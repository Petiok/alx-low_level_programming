#include "main.h"

/**
 * _isupper - Check is a letter is upper
 * @c: The number to be checked
 * Return: 1 for upper and 0 for otherwise
 *
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
{
	return (1);
	}
	return (0);

}
